# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/
env/

# Dados / geopackages (grandes, não versionar)
*.gpkg
*.gpkg-shm
*.gpkg-wal
dados/

# Exceção: dados PDM embutidos no deploy (leves — Qualificação do Solo +
# Condicionantes, extraídos do opendata e reempacotados). A app lê estes.
!dados_pdm/qs.gpkg
!dados_pdm/ccgd.gpkg

# Sistema
.DS_Store
Thumbs.db
desktop.ini
saidas/
