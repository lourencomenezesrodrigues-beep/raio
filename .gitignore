# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/
env/

# Dados / geopackages (grandes, não versionar)
*.gpkg
*.gpkg-shm
*.gpkg-wal
dados/

# Exceção: dados embutidos no deploy (leves). A app lê estes.
#  - PDM: Qualificação do Solo + Condicionantes (opendata.porto.digital)
#  - Overture: edifícios + ruas do Porto (para a moda da cércea / largura da rua)
!dados_pdm/qs.gpkg
!dados_pdm/ccgd.gpkg
!dados_pdm/edificado.gpkg
!dados_pdm/ruas.gpkg

# Sistema
.DS_Store
Thumbs.db
desktop.ini
saidas/
